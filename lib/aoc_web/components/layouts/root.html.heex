<!DOCTYPE html>
<html lang="en" class="[scrollbar-gutter:stable] [&:has([data-theme=dark])]:dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title suffix=" · Powered by Elixir & Phoenix">
      <%= assigns[:page_title] || "Advent of Code Solutions" %>
    </.live_title>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
  </head>
  <body class="bg-cyber-black text-cyber-primary font-mono antialiased selection:bg-cyber-accent selection:text-white overflow-x-hidden">
    <!-- Background Grid with Perspective -->
    <div
      class="fixed inset-0 pointer-events-none z-0"
      style="
            background-image: 
                linear-gradient(transparent 95%, rgba(0, 255, 159, 0.1) 95%),
                linear-gradient(90deg, transparent 95%, rgba(0, 255, 159, 0.1) 95%);
            background-size: 40px 40px;
            transform: perspective(500px) rotateX(20deg) scale(1.5);
            transform-origin: top center;
            opacity: 0.4;
         "
    >
    </div>

    <canvas
      id="matrix-snow"
      phx-hook="MatrixSnow"
      class="fixed inset-0 pointer-events-none z-0 opacity-60"
    >
    </canvas>
    <!-- Screen Effects -->
    <div class="scanlines"></div>
    <div class="crt-flicker"></div>
    <div class="vignette"></div>

    <div class="relative z-10 min-h-screen flex flex-col">
      <%= @inner_content %>
    </div>
  </body>
</html>
